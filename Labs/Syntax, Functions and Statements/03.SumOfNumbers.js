function solve(n, m) {
    let firstNumber = Number(n);
    let secondNumber = Number(m);
    let result;


    for (let i = firstNumber; i <= secondNumber; i++) {
        result =result + index;
    }

    console.log(result);
}

solve('1','3');